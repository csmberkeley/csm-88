In Python, we wrote programs using \define{imperative programming} -- where
code is written as a set of instructions for the computer.  In contrast, with
\define{declarative programming} our code declares \emph{what} result we want,
not \emph{how} to compute it. 

\\
